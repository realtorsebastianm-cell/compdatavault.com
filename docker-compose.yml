services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: dealarchive
      POSTGRES_USER: dealarchive
      POSTGRES_PASSWORD: dealarchive
    ports:
      - "5432:5432"
    volumes:
      - dealarchive_pgdata:/var/lib/postgresql/data

volumes:
  dealarchive_pgdata:
